Flask==3.0.3
pyotp==2.9.0
cryptography==42.0.8

# Needed only for the desktop app build (native window + packaging)
pywebview>=5.3.2
pyobjc>=10.3.1 ; sys_platform == "darwin"
pythonnet>=3.0.3 ; sys_platform == "win32"
pyinstaller>=6.10.0

# Linux also needs a web-rendering backend for pywebview, but it can't be
# reliably pip-installed across distros - install ONE of these yourself:
#   GTK (recommended): system package, e.g. `sudo apt install python3-gi gir1.2-webkit2-4.1`
#   Qt (pip-only alt): `pip install PyQt5 PyQtWebEngine`
# build_linux_app.sh detects which you have and offers to install the Qt
# fallback automatically if needed.
